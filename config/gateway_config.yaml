secure_telemetry_gateway_node:
  ros__parameters:
    db_path: "data/telemetry_history.db"
    max_rate_pps: 500.0
    burst_capacity: 1000.0
    enable_encryption: true
    worker_poll_interval_ms: 10